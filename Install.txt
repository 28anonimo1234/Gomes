
});

// Iniciar servidor
const PORT = 3001;
app.listen(PORT, () => {
  console.log(`Servidor rodando na porta ${PORT}`);
});
Passo 3 – Rodar servidor
No terminal:

bash
Copiar código
node server.js
Se tudo certo, tu vai ver:

yaml
Copiar código
Conectado ao MySQL ✅
Servidor rodando na porta 3001
Passo 4 – Consumir no front
No teu Vite (React ou Vanilla), tu pode chamar:

js
Copiar código
fetch("http://localhost:3001/clientes")
  .then(res => res.json())
  .then(data => console.log(data));
Com isso, teu projeto já conecta no MySQL e faz CRUD de Clientes.
Dali é só replicar a lógica pras outras tabelas (Produto, Venda, etc.).

